import React from 'react';

import './styles.scss';

const GuestCard = () => (
  <div styleName="guest-card">
    <p styleName="title">Please login or create an account to add an event.</p>
  </div>
);

export default GuestCard;
